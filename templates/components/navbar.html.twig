<nav class="navbar navbar-expand-sm bg-body-tertiary fixed-top opacity-75">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_travels_index') }}"><i class="fa-regular fa-compass fa-2xl"></i> Viva la Trip</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav align-items-center ms-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_travels_new') }}">Add a travel destination</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_logout') }}">Log out</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_login') }}">Log in</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_register') }}">Registration</a>
        </li>
        <li class="nav-item ms-3">
          {% if is_granted('') %}
          <p class="nav-link active d-inline">Hello, {{ app.user.firstName }}!</p>
          <img class="rounded-circle asprat object-fit-cover" src="{{asset('pictures/'~ app.user.picture)}}" alt="" width="40px" />
          {% elseif is_granted('ROLE_ADMIN') %}
          <p class="nav-link active d-inline">{{ app.user.firstName }}</p>
          {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>
