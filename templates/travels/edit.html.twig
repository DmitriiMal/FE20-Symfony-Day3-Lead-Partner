{% extends 'base.html.twig' %}

{% block title %}Edit Travels{% endblock %}

{% block body %}
    <h1>Edit Travels</h1>

    {{ include('travels/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_travels_index') }}">back to list</a>

    {{ include('travels/_delete_form.html.twig') }}
{% endblock %}
